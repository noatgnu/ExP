export class ExpTime {
  constructor(Days?: number, Hours?: number, Seconds?: number) {
    this.Days = Days;
    this.Hours = Hours;
    this.Seconds = Seconds;
  }
  Days: number;
  Hours: number;
  Seconds: number;
}
